# Post-Merge And Post-Closure Snapshot — 2026-04-01

This document is the **archival record** of the merge event that landed
PRs #89, #90, #91, #92, and #93 onto `main` on 2026-04-01.

It replaces the earlier pre-merge review snapshot that tracked open work and
captures the final `v1.28.1` baseline after the post-merge closure pass.

## What Landed

| PR | Version | Summary |
|----|---------|---------|
| #89 | v1.25.1 | Rate limit split, telemetry auto-recording, workflow/pipeline persistence, semantic cache hardening |
| #90 | v1.26.0 | CI architecture invariants (26 tests), retention pruning cron, nightly cleanup, unified policy migration (5/9 callers) |
| #91 | v1.27–v1.28.0 | Operator REST API, HTML dashboard, archival service, settlement foundation, 6 production regression fixes |
| #92 | v1.28.1 prep | Archive download, Telegram settlement command, doc truthfulness closure |
| #93 | v1.28.1 | Release merge and post-merge closure on `main` |

## Main Baseline After Merge

- `origin/main`: `f3f6b80`
- version: `1.28.1`
- tests: 1608 passed, 4 skipped

## What Is Delivered vs Foundation-Level

### Delivered (production-ready on main)

- Authenticated operator REST API under `/api/operator/`
- HTML dashboard at `/dashboard` with real-time metrics
- Archival CSV export plus authenticated CSV download with
  deployment-safe filename handling
- CI-enforced architecture invariants (26 blocking tests)
- Automated retention pruning (6h) + nightly data cleanup
- Telemetry auto-recording (hourly snapshots)
- Workflow + pipeline SQLite persistence
- Unified policy boundary (gateway, review, build callers migrated)
- Memory injection filtered by provenance + kind (truthfulness-first)
- Structured 400 responses on all operator API query params
- Telegram `/settlement`, `/settlement approve`, and `/settlement deny`
  operator actions

### Foundation-Level (wired, not full workflow)

- **Payment Settlement**: initialized in orchestrator, has API endpoint
  (`GET /api/operator/settlements`), Telegram command surface, and manual
  top-up path, but:
  - pending state is in-memory (lost on restart)
  - no automatic 402→check→topup→retry loop
  - no dashboard approval UI
  - no restart-safe settlement audit trail or recovery state
- **Operator workflow depth**: dashboard and API now expose real surfaces, but
  settlement is still a partial operator flow rather than a fully closed,
  self-healing payment-resolution loop

## Next Steps

1. Persist settlement requests, outcomes, and restart recovery state
2. Add active settlement workflow to the dashboard and operator API
3. Close the approval-backed 402→balance→topup→retry loop
