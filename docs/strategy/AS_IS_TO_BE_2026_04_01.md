# As-Is / To-Be Snapshot — 2026-04-01

This document captures the truthful execution snapshot across:
- `origin/main`
- open pull requests `#89`, `#90`, and `#91`
- the current local working tree on `feature/operator-api-archival`

Purpose:
- keep `BACKLOG_PROGRESS.md` and `NEXT_BACKLOG.md` honest as `main`-based docs
- describe what is already merged versus what is only proposed or locally fixed
- define the safest near-term merge path

## Assessment Basis

- `origin/main`: `03d87b8` (`Fix critical: inject stored memories into LLM prompt`)
- open PRs:
  - `#89` `v1.25.1: Production hardening batch`
  - `#90` `v1.26.0: Phase 4 enterprise hardening`
  - `#91` `v1.27.0–1.28.0: Operator API, dashboard, archival, payment settlement`
- local branch: `feature/operator-api-archival` — audit fixes committed at `626d061`

## As-Is On `main`

`main` is still the stable documented baseline.

What is already true on `main`:
- Phase 3 closure and later runtime/gateway work are present.
- Buyer-side Obolos/API-call capability work is present.
- The stored-memory hotfix from `03d87b8` is present.
- `BACKLOG_PROGRESS.md` and `NEXT_BACKLOG.md` are still correctly scoped as
  `main` documents.

What is **not** yet true on `main`:
- production hardening batch from PR `#89`
- phase 4 hardening batch from PR `#90`
- operator dashboard / archival / settlement slice from PR `#91`

## As-Is In Open Work

### PR `#89` — Production Hardening Batch

What it adds:
- localhost-friendly API rate limit split
- hourly telemetry recording
- recurring workflow + pipeline SQLite persistence
- tighter semantic cache filtering

Audit note:
- this is operationally valuable hardening and should not be treated as already
  delivered until merged

### PR `#90` — Phase 4 Enterprise Hardening

What it adds:
- CI architecture invariants
- retention pruning / cleanup loops
- broader `evaluate_runtime_action()` policy migration
- retention reporting in Telegram

Audit note:
- this PR materially strengthens runtime safety and should be considered part
  of the hardening baseline for any operator-facing release that follows it

### PR `#91` — Operator API, Dashboard, Archival, Settlement

What it adds:
- operator REST endpoints
- HTML dashboard
- archival CSV export service
- payment settlement service

Audit note:
- this PR is the most user-visible slice, but it currently depends on fixes
  that are only present in the local working tree
- it also does not yet include the hardening from `#89` and `#90`

## Current Audit Reality

### Committed On Branch (at `626d061` + subsequent)

These fixes are committed on `feature/operator-api-archival`:
- operator report endpoint uses `agent.reporting` (correctly wired)
- operator API integer query parsing returns structured `400` errors
- archival export writes under `data/archive/`, returns filename-only
- pipeline job linkage uses `control_plane` (not stale `control_plane_state`)
- memory injection filters by provenance (OBSERVED/USER_ASSERTED/VERIFIED)
  and kind (FACT/PROCEDURE); no blind latest-memory fallback
- PaymentSettlementService wired into orchestrator (`agent.settlement`)
- Settlement API endpoint: `GET /api/operator/settlements`

### Still Foundation-Level (Not Full Workflow)

- **PaymentSettlementService** is initialized in orchestrator and has an API
  surface, but it is still a foundation:
  - no automatic 402→check→topup→retry loop
  - no dashboard approval/execute UI
  - pending state is in-memory (lost on restart)
  - no Telegram command surface for settlement approval
- **Archival export** is safer but still list/export-filename only, not a
  download-safe operator evidence workflow
- PR `#91` on its own would ship operator surfaces without the
  stronger persistence/telemetry/invariant hardening proposed in `#89` and `#90`

## To-Be

### Recommended Merge Path

1. Land the local audit fixes onto `feature/operator-api-archival`.
2. Rebase or otherwise fold in the production hardening from `#89`.
3. Rebase or otherwise fold in the phase 4 hardening from `#90`.
4. Re-run full verification on the combined branch.
5. Only then publish the next release/tag.

### Recommended Truthfulness Rule

Until the above merge path is complete:
- keep `BACKLOG_PROGRESS.md` and `NEXT_BACKLOG.md` phrased as `main` snapshots
- treat this document as the truthful review-time snapshot for open work
- do not describe dashboard/settlement/archival or hardening slices as shipped
  on `main`

### Near-Term To-Be State

The desired near-term merged state is:
- operator endpoints that are runtime-safe and handler-tested
- archival export that is instance-data-aware and non-leaky
- pipeline/workflow persistence and telemetry recording from `#89`
- retention cleanup and architecture invariants from `#90`
- a settlement workflow that is either:
  - fully wired into orchestrator/API/dashboard and approval state, or
  - explicitly downgraded in docs from "workflow" to "service foundation"

## Practical Interpretation

Today:
- `main` is the stable documented baseline
- `#89`, `#90`, and `#91` are meaningful progress, but still open work
- the local working tree already contains important audit fixes that should be
  preserved before any merge decision

The next honest release should be built from:
- `#91` after the local fixes are committed
- plus the hardening slices from `#89` and `#90`
- with docs updated only after that combined branch is verified
